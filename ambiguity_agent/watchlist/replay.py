"""MOHITA. Temporal replay. Without this, nothing happens while the judges watch."""


class ReplayClock:
    def run(self, start, end, speed: int = 1000):
        raise NotImplementedError("stream orders forward. show at least one REVOCATION -> "
                                  "tier DE-ESCALATES. nobody else builds de-escalation.")
